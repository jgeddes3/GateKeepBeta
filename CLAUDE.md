# GateKeep

Start every fresh session by reading `docs/superpowers/HANDOFF.md`: it holds the project state,
the binding design and copy rules (including: no em dashes anywhere), the dev-environment
quickstart, and pointers to the per-sub-project rulings docs that are the authority for each
area. `DESIGN.md` at the repo root is the binding brand contract for all UI work.
