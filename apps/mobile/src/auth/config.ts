// Google sign-in OAuth "Web SDK configuration" client id, from Firebase console →
// Authentication → Sign-in method → Google → Web SDK configuration. This value is NOT
// retrievable via the Firebase CLI (it's an OAuth client id, not part of the app SDK config)
// and must be pasted in manually from the console.
export const GOOGLE_WEB_CLIENT_ID = "REPLACE_FROM_FIREBASE_CONSOLE.apps.googleusercontent.com";
